# FastAPI and Server
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
pydantic-settings==2.1.0

# Google Cloud
google-cloud-storage==2.14.0
google-auth==2.25.2

# ML Framework Support
# These are installed via user requirements.txt or framework-specific base images
# Avoid hardcoding versions here to prevent conflicts with user models
# scikit-learn>=1.3.2  # For sklearn models (install via user requirements)
# torch>=2.0.0  # For PyTorch models (install via user requirements)
# tensorflow>=2.15.0  # For TensorFlow models (install via user requirements)
# onnxruntime>=1.16.0  # For ONNX models (install via user requirements)

# Utilities
python-multipart==0.0.6  # For file uploads
pillow==10.1.0  # For image processing
aiofiles==23.2.1
# Note: numpy is installed via ML framework or user requirements to avoid version conflicts

# Monitoring & Logging
python-json-logger==2.0.7

# Security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
